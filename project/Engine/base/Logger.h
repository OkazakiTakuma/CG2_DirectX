#include <string>
#include <dxgidebug.h>
namespace Logger {
/// <summary>
/// Log の処理を行います。
/// </summary>
/// <param name="messeage">messeage に使用する値を指定します。</param>
void Log(const std::string& messeage);
}
